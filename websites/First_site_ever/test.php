	<!--require_once "dbconnect.php"; 
	
	$db_connection = @new mysqli($host,$db_user,$db_password,$db_name);
	
	if($db_connection->connect_errno!=0)
	{
		echo "Error: ".$db_connection->connect_errno." Description: ".$db_connection->connect_error;
	}
	else
	{--><!--$db_conncetion->close();
	}-->